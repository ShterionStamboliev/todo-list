export {}

declare global {
    
    type TodoProps = {
        owner?: string;
        title: string
    }

    // type InputProps = {
    //     text: string
    // }

    // type IProps = TodoProps & InputProps
}